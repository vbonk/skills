# Publish Package — Push to GitHub Packages on release
# Uncomment the section matching your language. Supports npm, PyPI, Docker.
# Triggers on version tags (v*) created by the release workflow.
#
# IMPORTANT: SHA-pin all actions. Do not use version tags.

name: Publish Package

on:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      # === npm (Node.js/TypeScript) ===
      # - name: Setup Node.js
      #   uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
      #   with:
      #     node-version: 22
      #     registry-url: https://npm.pkg.github.com
      #     scope: '@${{ github.repository_owner }}'
      #
      # - name: Install and build
      #   run: npm ci && npm run build
      #
      # - name: Publish to GitHub Packages
      #   run: npm publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # === Docker (Container Registry) ===
      # - name: Login to GitHub Container Registry
      #   uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Build and push Docker image
      #   uses: docker/build-push-action@4f58ea79222b3b9dc2c8bbdd6debcef730109a75 # v6.9.0
      #   with:
      #     push: true
      #     tags: |
      #       ghcr.io/${{ github.repository }}:latest
      #       ghcr.io/${{ github.repository }}:${{ github.ref_name }}

      # === Placeholder (remove when uncommenting above) ===
      - name: No package type selected
        run: echo "Uncomment the npm or Docker section in publish-package.yml"
